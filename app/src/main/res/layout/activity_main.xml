<?xml version="1.0" encoding="utf-8"?>
<!-- TODO: Design the main screen for displaying today's bookings -->
<!-- This layout should include:
     - A title "Today's Bookings" at the top
     - A RecyclerView to display the list of bookings (ID: @+id/recyclerViewBookings)
     - A TextView for showing "No bookings today" when list is empty (ID: @+id/textEmptyState)
     - Optional: A button to view bookings for other dates
     - Optional: SwipeRefreshLayout for pull-to-refresh functionality

     The RecyclerView should take most of the screen space.
     The empty state message should be centered and only visible when there are no bookings.
-->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!-- TODO: Add UI components here -->
    <!-- Example structure:
         1. Title TextView at the top
         2. RecyclerView below the title
         3. Empty state TextView (initially hidden)
    -->

</androidx.constraintlayout.widget.ConstraintLayout>